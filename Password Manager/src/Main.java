
public class Main {

	public static void main(String[] args) throws InterruptedException {

		//First check if a password already exists on the computer.
		// If a password exists it will jump to the normal login object if not
		// The user will be lead through a new password setup
	newLogin userEnterance = new newLogin();
	newLogin login = new Login();
	//After login is verified
	Menu user = new Menu();
	}
}